[build-system]
requires = ["setuptools >= 77.0.3"]
build-backend = "setuptools.build_meta"

[project]
name = "client_certificate_middleware"
readme = "README.md"
version = "1.0.2"
requires-python = ">= 3.8"
dependencies = [
    "fastapi[standard]",
    "cryptography"
]